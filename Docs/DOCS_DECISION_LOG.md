# CoreStaff — Scope Decision Log

> Ghi nhận các quyết định resolve mâu thuẫn thuộc QC_REVIEW_CORESTAFF_DOCS.md. SRS v4.0 là source of truth.
> Ngày chốt: 12/09/2026. Mỗi dòng: Decision / Date / ảnh hưởng docs.

| ID | Decision | Giá trị chốt | Tác động |
|---|---|---|---|
| D01 (QC-001) | OT pay | **Payroll tính tiền OT** theo OvertimePayPolicy; Attendance/Timesheet chỉ lưu phút (requested/approved/actual/eligible) | FR-OT-03, 30D.2 |
| D02 (QC-002) | Payroll bên trong/ngoài | **Tính lương nội bộ** qua PayrollRun/Payslip | Proposal §5.3 |
| D03 (QC-003) | Must Have | Thêm Flow 4A (Payroll) + 4B (OT) + Adjustment (before close) vào Must Have | Proposal §1, §16 |
| D04 (QC-004) | Leave | **Cập nhật 12/09/2026:** Employee → Manager duyệt → HR apply. **NC-1 = A:** MVP **cấm** HR CRUD `EmployeeDayOverride` ngoài apply. Quota/accrual/balance = LATER | **Synced:** FR-HRCFG-05, FR-LEAVE-03, BR-LEAVE-03, API, UC-13, routes §14 |
| D05 (QC-005) | Expected outcomes | Bỏ "schedule registration/swap" | Proposal §15 |
| D06 (QC-006) | Adjustment | Before-close = MVP; After-close = qua reopen (giữ tách bạch) | Proposal Flow 5, SRS FR-ADJ |
| D07 (QC-007) | RecurringSchedule | **Chỉ HR CRUD**; Manager chỉ xem department scope | FR-SCH-02 |
| D08 (QC-008) | Reset mật khẩu | HR reset trong Organization mình; System Admin reset nền tảng/HR đầu tiên | FR-AUTH-04 |
| D09 (QC-009) | employmentType | Field thuộc `EmployeeProfile`, không nằm trên User | §15.2, 29A.2 |
| D10 (QC-010) | Tên entity | Duy nhất `LaborCompliancePolicy` (có probationMinimumRate) | 29A.3, 29B, 29F |
| D11 (QC-011) | Event REJECTED | Là blocker chốt kỳ; resolve qua resubmit/clarification hoặc HR quyết dayResult | FR-HR-02, BR, §7.2 |
| D12 (QC-012/028) | Payslip release / PAID | Payslip phát hành khi Payroll `LOCKED`; `PAID` = HR xác nhận chi trả thủ công (paidAt + note), không ngân hàng | 29E, AC-PAYROLL |
| D13 (QC-013/014) | Role naming | Enum: `SYSTEM_ADMIN`, `HR`, `DEPARTMENT_MANAGER`, `EMPLOYEE`; display: System Admin / HR (Payroll Officer) / Department Manager / Employee | Proposal §5, glossary |
| D14 (QC-018) | Use Case numbering | Đánh lại UC-11 (OT), UC-12 (tenant isolation) | SRS §13 |
| D15 (QC-041) | overallApprovalStatus | Ưu tiên: REJECTED > CLARIFICATION_REQUESTED > PENDING > APPROVED > NOT_REQUIRED | §7.2, AC |
| D16 (QC-042) | Export | CSV kỳ chốt = Must Have (thuộc Flow 4); custom report export = SHOULD | Proposal Flow 6 |
| D17 (QC-045) | GPS anomaly | MVP chỉ auto-approval cho SELFIE; GPS anomaly = SHOULD | Proposal Flow 3, FR-MGR-03 |
| D18 | Chuyên cần (Attendance bonus) | Hệ thống cung cấp **cách tính mẫu** (vd tier % 100/70/50, rule đi trễ…). **HR từng Organization tự setup** policy + tự thêm điều kiện tính; **không hard-code** rule công ty trong code | **Synced 12/09/2026:** FR-COMP-01, models/APIs/AC, TASK-034/088 |
| D19 | Phụ cấp (Allowance) | **Hybrid:** seed **library mặc định** (ăn trưa, xăng xe, điện thoại…) để HR chọn/bật; HR vẫn **tạo thêm mục phụ cấp riêng** trong Organization | **Synced 12/09/2026:** FR-COMP-02, models/APIs/AC, TASK-033/088 |

| D20 / D29 | Technology stack (updated) | **NestJS + MongoDB replica set + ReactJS Web MVP**; **React Native + Expo Employee app = SHOULD** | **Resolved:** SRS §23.2/OQ-01/DoD, Proposal §11, Milestone/Backlog |
| D21 | Approval delegation | Ưu tiên `ApprovalDelegation` active; fallback HR queue chung; actorId != employeeId | **Resolved 12/09/2026:** model/routing rule |

| D30 | Đóng băng Milestone và Task Backlog | Từ sau D30, `MILESTONE_9_WEEKS.md` và toàn bộ TASK-001…TASK-118 là immutable baseline. Chỉ được append task mới từ TASK-119; không sửa/xóa/đổi ID/nội dung/module/Sprint/owner/support/priority/status/date/progress/hours/dependency/link/note/deadline status của task cũ; không sửa cấu trúc hoặc nội dung milestone | Governance rule có hiệu lực ngay |

| D31 | Bỏ Docker khỏi triển khai | **Không dùng Docker** để deploy. MongoDB chạy qua **Atlas (replica set managed)**; API/Web deploy bằng HTTPS hosting trực tiếp (không container). Xóa `docker-compose.yml`. Thay TASK-110 (Docker Compose clean run) bằng Atlas + HTTPS hosting. | **Synced:** SRS §23.2, Proposal §11, wiki, CORESTAFF_REPO_STRUCTURE.md; append TASK-119 |

| D32 | Provisioning API (§4.1) | HR tạo employee = **một lời gọi atomic** `POST /api/hr/employees` (không `userId`) → tạo `User` role `EMPLOYEE` (ACTIVE, `mustChangePassword: true`) + `EmployeeProfile` (PROBATION) trong một transaction; password tạm do server sinh, **trả về một lần, không persist, không log**, chỉ hash lưu DB (DoD `:2352`). `fullName`/`email` bắt buộc khi tạo tài khoản — enforce ở service, không ở DTO (DTO dùng chung cho route `/me`). `role` không có trên DTO → HR **không mint** được role khác (§16.6) nhờ whitelist pipe. | **Synced:** FR-HRCFG-02 `:648`, HR endpoint, AC; append TASK-120+ |
| D33 | Platform module (SYSTEM_ADMIN) | `@PlatformOnly()` — bỏ bỏ bypass SYSTEM_ADMIN mặc định của `RolesGuard` khi route mang metadata này (AC-SYS-01). Module `platform/organizations`: `GET /`, `POST /` (tạo org, 11000 → `ORGANIZATION_CODE_TAKEN`), `POST /:id/initial-hr` (chỉ `{email, fullName}` — **không `employeeCode`**, role HR hardcode, `mustChangePassword: true`), `POST /:id/suspend|activate`. **Không tự tạo HR profile** khi initial-hr — tránh cấp `attendance:self` ngầm toàn tenant (§16.2 `:162`). Suspend: đảo status + revoke mọi session còn sống; login check org LOCKED → 423 `TENANT_SUSPENDED` (AC-SYS-02 / BR-AUTH-03). | **Synced:** FR-SYS-01/02, AC-SYS-01/02, BR-AUTH-03, appendix; append TASK-120+ |
| D34 | `employeeCode` thuộc `EmployeeProfile` **(deviation)** | **Deviation khỏi §15.2 `:1274` `UNIQUE INDEX(organizationId, employeeCode)`**: code độc nhất per tenant chỉ nằm trên `EmployeeProfile`; `User` bỏ `employeeCode` (prop + index + pre-validate hook). Login theo code đi qua profile join, tenant-scoped, case-fold (normalize `toUpperCase()`) — hết trùng `TVS-0001`/`tvs-0001`. Index cũ trên `users` được `ensure-indexes` drop + purge field. UI đọc `employeeCode` từ profile join trong `toSafeUser`, không đổi shape. | **Deviation §15.2 `:1274`; Synced:** §15.2A, auth login, login-by-code AC, index contracts, UI sites; append TASK-120+ |
| D35 | HR tự tạo profile riêng | HR/Department Manager **tự tạo profile của mình** qua `POST /api/hr/employees/me`: actor `userId` từ session (không từ body; co body `userId` → 400 `USER_ID_NOT_ALLOWED`), copy `email`/`phone` từ account (không từ payload, giữ 2 doc đồng bộ TASK-120/A3), PROBATION. DoD `:631` chỉ mint *User* của HR, FR-HRCFG-02 `:648` chỉ cho HR tạo Employee/Department Manager — không ai khác điền hồ sơ cho HR được. Kèm guard: `changeStatus` actor == profile.userId → 403 `SELF_APPROVAL_FORBIDDEN` (AC-SELF-APPROVAL-01 `:2054`); `listEligibleUsers` loại chính mình. | **Synced:** FR-SYS-02 `:631`, FR-HRCFG-02 `:648`, AC-HR-SELF-01, Route graph; append TASK-120+ |

| D36 | Department Manager workspace, scope và delivery order | `DEPARTMENT_MANAGER` giữ 3 chức năng cá nhân (Chấm công, Lịch sử, Nghỉ phép & OT) và có thêm **Phòng ban** với 2 tab **Phê duyệt** + **Đánh giá nhân sự**. Phê duyệt chỉ xử lý Selfie/ngoại lệ/adjustment/clarification + OT, không duyệt mọi Network/GPS hợp lệ. Đánh giá nhân sự MVP = KPI kỳ lương (Manager DRAFT, HR CONFIRMED). Scope lấy từ `ManagerAssignment` có hiệu lực, hỗ trợ nhiều phòng, không suy từ profile. Hoàn thiện/nghiệm thu ReactJS responsive desktop + mobile web trước; chỉ sau đó port sang Expo bằng cùng API. | **Synced:** SRS FR-MGR-08..11/routes/model/API/errors/AC/DoD; Proposal; Context; Use Case; Design Master; wiki; append TASK-120..126 |

| D37 | Phạm vi TASK-038/039 (InsuranceProfile, InsurancePolicy rate/base/caps) | TASK-038/039 phụ thuộc `insuranceSalary` (SalaryProfile, §30D.1) và trạng thái hợp đồng (EmploymentContract, §30A.2). Ban đầu (2026-09-22) đã tự dựng bản EmploymentContract/SalaryProfile tối giản làm nền; sau khi pull `origin/deploy` phát hiện team đã có sẵn bản đầy đủ hơn (`hr/employment-contract`, `hr/compensation`) — đã **bỏ 2 module tự dựng đó**, chỉ giữ `InsuranceProfile`/`InsurancePolicy` (không ai làm trùng phần này). Field-level spec của `InsuranceProfile` và shape của `salaryBaseRules`/`capRules` **không có sẵn trong Docs/** (xác nhận qua audit 2026-09-22) — mọi field/shape thêm vào SRS §30A.2/§30D.3/§30D.3A là **đề xuất kỹ thuật**, giá trị rate/floor/cap để `null`/chờ HR-legal xác nhận, không hard-code số liệu pháp lý. | **Synced 2026-09-22:** SRS §30D.3, §30D.3A, §30G, §30H, §30I (AC-INS-02..06); code tại `CoreStaff/Apps/api/src/hr/{insurance-profile,insurance-policy}`; không sửa `TASK_BACKLOG_9_WEEKS.md`/`MILESTONE_9_WEEKS.md` (D30 freeze) — xem `Docs/TASK_038_039_IMPLEMENTATION.md` cho nhật ký triển khai |

| D38 | Phạm vi TASK-068..071 (OT self-classification, eligible minutes, giới hạn lao động, tests) | **Deviation đặt tên §15.11**: `OvertimeRequest` **không** thành collection riêng — `manager_requests{type:'OVERTIME'}` (TASK-066/067, đã có code) vẫn là request of record, **chỉ thêm** `overtime_results`; field `overtimeRequestId` giữ nguyên tên để một lần tách sau này chỉ đổi `$ref`. Không migration. Kết quả tính **chỉ sinh tại lúc duyệt** (lúc submit chỉ lưu window) vì nhân viên *report OT sau khi đã làm* — punch đã có, eligible chưa chắc. **Retroactive**: grace window 1 ngày (`RETROACTIVE_GRACE_DAYS`) thay vì gate cứng; ngoài hạn mới bắt buộc `retroactiveReason` (FR-OT-01 không định nghĩa "trễ"). **§30B.2**: BLOCK = 422 `OVERTIME_*_LIMIT_EXCEEDED` tại duyệt và **trước** ghi (request phải còn PENDING/version cũ); tại submit chỉ **projection từ `requestedMinutes` → WARNING**, không block, vì số thật chưa tồn tại; policy theo `workDate` chứ không theo hôm nay. `LaborCompliancePolicy` thiếu ⇒ **tolerate ở submit** (request đã ghi, lỗ hổng cấu hình HR không được báo là "từ chối công đã làm" — trả `compliance:null` + `complianceNote`), **đòi ở approve** (404 `LABOR_POLICY_NOT_FOUND`). Result lưu `policyVersion`+`legalReference` (§30B.2, ngoài field list §15.11) và `inputHash` để staleness **suy ra trên read** thay vì fan-out ghi lại (BR-OT-05). Break **không** bị trừ riêng khỏi OT — đã nằm trong scheduled interval bị trừ, nên không phút nghỉ nào thành OT. `normalDaily` cap theo `scheduledMinutesOf` (net break: 08:00–17:00/60 ⇒ 480, khớp seed §30B.1). **7 spec gap tự chốt**: (1) ngày `PAID_LEAVE`/`UNPAID_LEAVE` có punch ⇒ `OT_WORKING_DAY`; (2) `WEEKLY_OFF`/no-schedule (shift null, không exception) ⇒ `OT_WEEKLY_OFF`; (3) `SPECIAL_WORKING_DAY` trên ngày nghỉ tuần ⇒ coi như ngày làm; (4) phút dùng `Math.floor`, không làm tròn lên; (5) OT qua nửa đêm quy về `workDate`, phần vượt cắt + `CROSS_MIDNIGHT_CLAMPED` (không ca đêm MVP, §30J); (6) 2 request PENDING/APPROVED trùng cửa sổ cùng ngày ⇒ 409 `OVERTIME_OVERLAP` ngay lúc tạo; (7) TOCTOU giữa precheck và ghi (2 manager cùng vượt monthly cap) **chấp nhận cho MVP**, `ponytail:` comment, guard thuộc Sprint 6. Không tạo `TimesheetPeriod`/`periodId`/confirmation (Sprint 6); không sửa `Apps/web` (conflict branch `UI-Webs`). **Caveat §30K**: `maxCombinedDailyMinutes: 720` / `maxMonthlyOvertimeMinutes: 2400` trong seed là **giá trị demo, chưa HR-legal xác nhận** — test không hard-code ngưỡng nào, mỗi test tự hạ một con số của policy được seed. | **Code + test 2026-09-26:** `CoreStaff/Apps/api/src/{common/vietnam-time,database/schemas/overtime-result,hr/overtime}/*` + `Apps/api/test/*` (lane integration mới, MongoMemoryReplSet); unit 47 suites/482 tests, integration 2 suites/26 tests; chưa sync SRS §15.11/§16.6C field-level (đợi §30K chốt số pháp lý); không sửa `TASK_BACKLOG_9_WEEKS.md`/`MILESTONE_9_WEEKS.md` (D30 freeze) — xem `Docs/TASK_068_071_IMPLEMENTATION.md` |
| D39 | Trần nộp OT tuyệt đối + cấm lấn ca + phát hiện OT chưa báo | **Không đảo D38** — nghiệp vụ vẫn là *báo sau khi làm*. Thêm phần còn thiếu: (1) **`maxRetroactiveFilingDays`** trên `LaborCompliancePolicy` (default `DEFAULT_MAX_RETROACTIVE_FILING_DAYS = 7`, seed cùng giá trị để HR thấy là knob) — ngoài trần này từ chối thẳng 409 `OVERTIME_FILING_WINDOW_CLOSED`, `retroactiveReason` dài bao nhiêu cũng không mua được; grace 1 ngày của D38 giữ nguyên, nằm *trong* trần. Lý do: không có trần thì cap §30B chỉ đo phút *đã duyệt*, và phút đã duyệt có thể được khai lại vô hạn sau khi sự đã rồi. (2) **OT không được lấn ca được gán** — 409 `OVERTIME_OVERLAPS_SCHEDULE`, chặn **cả lúc gửi lẫn lúc duyệt** (manager nới `approvedStart`/`approvedEnd` cũng phải sạch), rule duy nhất là `minutesInside(window, scheduledIntervals) > 0`; chạm biên được vì interval half-open. Guard dùng **đúng** `scheduledIntervals()` mà calculator trừ, nên thứ bị chặn chính là khung có `eligibleMinutes = 0` — không phát sinh định nghĩa "ca hành chính" thứ hai có thể drift; ngày không resolve ra ca (nghỉ tuần/lễ/không lịch) **tự động pass**, không cần `isAdministrative` (flag không tồn tại). Không hard-code 08:00–17:00 ở đâu (BR-SHIFT-01). (3) **OT làm mà không báo**: lúc check-out, `unreportedOvertimeMinutes` = punch − scheduled − các window đang PENDING/APPROVED; > 0 thì push `compliance:warning {reason:'UNREPORTED_OVERTIME'}` fire-and-forget (không chặn chấm công). Thứ tự guard lúc gửi: **schedule → overlap → retro** (báo cái user sửa được trước, quá khứ bất khả thi sau). Route mới `GET /api/overtime/schedule?date=` cho web hỏi đúng resolver mà guard dùng, thay vì client tự suy ra precedence DEPARTMENT > ORGANIZATION. **Reversal của D38**: D38 ghi "không sửa `Apps/web`"; lần này sửa vì form cũ POST `/api/requests` (không qua projection §30B.2) và không có input nào cho `workDescription`/`retroactiveReason` — nay OT đi `POST /api/overtime`, hiển thị ca được gán, báo sớm lỗi lấn ca và render `compliance.violations`. Toàn bộ mã lỗi OT/§30B dịch tiếng Việt qua `HR_ERROR_CODES`. Vẫn **không** có gate cứng "phải xin trước khi làm" — đó là đổi flow thật, chưa §30K chốt. | **Code + test 2026-09-26:** `hr/overtime/{overtime.service,overtime-domain,overtime.controller}.ts`, `hr/manager/manager-request.service.ts`, `attendance/attendance.service.ts`, `database/schemas/{compensation,manager-request}.schema.ts`, `database/seed/seed.ts`; `Apps/web`: `Employee/EmployeeWorkScreens.tsx`, `ManagerDepartment/ManagerDepartmentScreen.tsx`, `services/{manager.service,hrService}.ts`. Test: unit `overtime.service.spec.ts`/`overtime-domain.spec.ts`/`manager-request.service.spec.ts` + lane integration mới `Apps/api/test/overtime-filing-rules.integration.spec.ts` (9 test HTTP, chứng minh guard chạy *trước* ghi: từ chối ⇒ 0 document, duyệt hỏng ⇒ request còn PENDING/version cũ). Deferred: field-level SRS §16.6C/§22.1 (theo D38, đợi §30K); `Docs/TASK_068_071_IMPLEMENTATION.md` là nhật ký. |

Các QC Low (chính tả, format, slug) không ghi ở đây; sẽ xử lý trực tiếp trong file.

## Ghi chú bổ sung (12/09/2026)

### Leave (D04 cập nhật)
```text
Employee tạo LeaveRequest
→ Department Manager approve/reject (đúng department scope; không tự duyệt)
→ HR apply → tạo EmployeeDayOverride (PAID_LEAVE / UNPAID_LEAVE) gắn leaveRequestId
→ đưa vào TimesheetSummary / PayrollInputSnapshot khi chốt kỳ

MVP: không có đường HR ghi override thẳng ngoài LeaveRequest apply.
```

### Chuyên cần (D18)
- Platform/seed: template phương pháp tính (percent tiers, late/early counters…).
- HR: chọn template, nhập mức thưởng, thêm/bớt điều kiện theo công ty.
- Engine payroll chỉ đọc policy version có hiệu lực + dữ liệu snapshot công.

### Phụ cấp (D19) — khuyến nghị đã chốt hướng
- **Không chỉ library cứng** (thiếu linh hoạt multi-tenant).
- **Không chỉ HR tự gõ từ trắng** (lệch tên, khó báo cáo chuẩn).
- **Hybrid:** library seed + HR custom trong tenant.
